# Backend Migrations (BIGINT / Long ID)

This directory is intended to store SQL migration scripts added for this PR.

- `mysql_users_bigint.sql`
- `postgres_users_bigint.sql`

Rollback steps and test instructions are included inside each script.

